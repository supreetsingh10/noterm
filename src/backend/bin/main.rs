fn main() {
    // cloning because, I will not modifying the data that is being parsed.
    // So it will not be a mutually shared modifiyable state.

    // this is backend crate, now sticky_term crate will be taking care of it.
    // start working with UI crate.
}
